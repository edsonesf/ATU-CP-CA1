<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CoffeeShopApp.Views.ColdDrinksPage"
             Title="Cold Drinks"
             BackgroundColor="#2C1A0E">

    <CollectionView ItemsSource="{Binding ColdDrinks}"
                    Margin="16">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid ColumnDefinitions="100,*,Auto"
                      Padding="8"
                      Margin="0,4">
                    <Image Source="{Binding ImageName}"
                           HeightRequest="80"
                           WidthRequest="90"
                           Aspect="AspectFill" />
                    <VerticalStackLayout Grid.Column="1"
                                         VerticalOptions="Center"
                                         Padding="12,0">
                        <Label Text="{Binding Name}"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="White" />
                        <Label Text="{Binding Price, StringFormat='€{0:F2}'}"
                               FontSize="14"
                               TextColor="#D4A96A" />
                    </VerticalStackLayout>
                    <Button Grid.Column="2"
                            Text="+"
                            FontSize="22"
                            FontAttributes="Bold"
                            BackgroundColor="#6F3D1F"
                            TextColor="White"
                            WidthRequest="44"
                            HeightRequest="44"
                            CornerRadius="22"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.AddItemCommand}"
                            CommandParameter="{Binding .}" />
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
